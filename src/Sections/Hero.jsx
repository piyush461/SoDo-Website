import Header from "../Components/Header";
import HeroSection from "../Components/HeroSection";

const Hero = () => {
  return (
    <div>
      <HeroSection/>
    </div>
  );
};

export default Hero;
