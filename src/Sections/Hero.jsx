import Header from "../Components/Header";
import HeroSection from "../Components/HeroSection";

const Hero = () => {
  return (
    <div>
      <Header />
      <HeroSection/>
    </div>
  );
};

export default Hero;
